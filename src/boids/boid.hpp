#pragma once
#include "p6/p6.h"

class Boid {
private:
    glm::vec2 m_coord; // m_z
    // bool  m_neighbor;

public:
    // TODO template ?
    Boid(glm::vec2 coord)
        : m_coord(coord) {}

    // Methods
    //  //Behaviour
    // Cohesion

    // Separation

    // Alignment
};
